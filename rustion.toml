# Rustion Server Configuration

# Listen address and port
# Supported formats:
# - "localhost:2222"
# - "*:2222" (binds to all interfaces)
# - "2222" (binds to all interfaces on port 2222)
# - "192.168.1.1:2222"
listen = "127.0.0.1:2222"

# Path to the server private key file
# If the file doesn't exist, a random key will be generated
server_key = "server_key.pem"

# Maximum number of authentication attempts per client
max_auth_attempts_per_conn = 5

# Log level for the application
# Valid levels: error, warn, info, debug, trace
log_level = "info"

# Global ip attempts count
max_ip_attempts = 100

# Global user attempts count
max_user_attempts = 30

# Used to unban global ip and user
unban_duration = "15m"

# Enable re-use target connection
reuse_target_connection = true

# Max idle time that the target connection should be removed
# Idle time range is between 1 minute and 24 hours
target_cache_duration = "30m"

# Time after which an inactive SSH session is disconnected
# Helps free up resources from idle connections
# Default: none (no timeout)
# inactivity_timeout = "1h"

# Time to wait before sending authentication rejection response
# This helps prevent brute-force attacks by slowing down failed auth attempts
# Default: 1s
auth_rejection_time = "1000ms"

# SSH server identification string sent to clients
# This appears in client logs and SSH banner
# Default: SSH-2.0-rustion_<version>
# server_id = "SSH-2.0-rustion"

# SSH client identification string sent to target servers
# This appears in target server logs when rustion connects
# Default: SSH-2.0-rustion_<version>
# client_id = "SSH-2.0-rustion"

# Important key, don't share and lose
secret_key = "2MMVPJvaDcwBecSOwvG1/fJueEmJHdNuJq3DoyUQ5I0="

# Enable recording interactive with targets
# This only work if pty requested
# Default: false
enable_record = false

# Enable recording input from user
record_input = false

# Record path for store asciinema file
# Default: ./record
record_path = "./record"

[database]
type = "sqlite"
path = "rustion.db"
